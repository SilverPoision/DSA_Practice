int lenght = sizeof(str) / sizeof(str[0]);

  // for (int i = 0; i < length; i++)
  // {
  //   if (ascii[str[i]] != -1)
  //   {
  //     ascii[str[i]] = -1;
  //   }
  //   else if (ascii[str[i]] == -1)
  //   {
  //     str[i] = "/0"
  //   }
  // }
  // return str.replace("/0", "");